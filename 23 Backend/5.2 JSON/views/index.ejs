<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" id="test" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">
    <% if (typeof recipe === 'undefined' || recipe === -1) { %>
      <h2>Pick your favourite taco ingredient 👆</h2>
    <% } else { %>
      <h2><%= recipeData[recipe].name %></h2>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li><%= recipeData[recipe].ingredients.protein.name %>, <%= recipeData[recipe].ingredients.protein.preparation %></li>
        <li><%= recipeData[recipe].ingredients.salsa.name %> (Spiciness: <%= recipeData[recipe].ingredients.salsa.spiciness %>)</li>
        <% recipeData[recipe].ingredients.toppings.forEach(topping => { %>
          <li><%= topping.quantity %> of <%= topping.name %></li>
        <% }); %>
      </ul>
    <% } %>
  </div>
  
</body>

</html>